# Repo-scoped Codex defaults. No secrets.

model = "gpt-5.5"
model_reasoning_effort = "medium"
plan_mode_reasoning_effort = "high"

# Personal sandbox, approval, MCP, and profiles belong in ~/.codex/config.toml.
